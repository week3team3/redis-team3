# 🗺️ 주요 기능별 소스코드 위치 안내 (발표용 가이드)

발표 도중 특정 기능이 코드 레벨에서 어떻게, 어디에 구현되어 있는지 즉각적으로 보여줄 수 있도록 준비된 **코드 네비게이션 가이드**입니다. IDE(VSCode 등)에서 해당 라인으로 바로 이동하여 심사위원들에게 설명하시면 효과가 매우 좋습니다.

---

### 1. 🛡️ 코어 자료구조 및 락 (Lock) 로직
모든 동시성을 단일 스레드와 자물쇠 환경에서 제어하는 **아키텍처의 핵심 심장부**입니다.
- **`Entry` (생명주기/메타데이터를 품은 커스텀 딕셔너리 값)**: `server.py` **38 Line** (`class Entry:`)
- **`store_lock` (전역 동시성 제어 자물쇠)**: `server.py` **69 Line** (`store_lock = threading.RLock()`)

### 2. ⏱️ 세션 방치 방지 및 좌석 TTL (Expiration)
사용자가 도망가거나 결제하지 않았을 때 서버가 스스로 판단하여 좌석과 대기열을 회수하는 **자동 가비지 컬렉터(GC) 로직**입니다.
- **10초 무반응 세션 자동 튕김 설정값**: `server.py` **32 Line** (`SESSION_IDLE_TTL = 10`)
- **타임아웃(유저 접속 끊김 / 좌석 찜 만료) 자동 청소**: `server.py` **336 Line 부근** (`def cleanup_ticket_event_locked()`)

### 3. 🌉 브릿지 서버 및 RESP 파싱 아키텍처
HTTP API와 TCP 소켓(RESP)의 언어 장벽을 완벽한 **3-Tier 브릿지 구조**로 극복해낸 것을 어필할 수 있는 곳입니다.
- **웹 서버 중계기 (JSON -> RESP 프록시 역할)**: `client.py` **252 Line** (`class TicketingBridgeHandler`)
- **API -> RESP 바이너리 직렬화 인코더**: `client.py` **72 Line** (`def build_request()`)
- **RESP 바이트 파서 (서버 수신부 디코더)**: `server.py` **667 Line 부근** (`def parse_resp()`)

### 4. 💾 영속성 및 스냅샷 (Persistence 방벽)
서버가 셧다운되더라도 대기열과 예매된 좌석이 날아가지 않도록 **Point-in-Time 디스크 덤프**를 진행하는 로직입니다.
- **인메모리 해시 데이터 통쨰로 덤프(저장) 뜨기**: `server.py` **96 Line** (`def save_snapshot_locked()`)
- **서버 켜질 때 스냅샷 로드(복구) 하기**: `server.py` **74 Line** (`def load_snapshot()`)

### 5. 🎟️ 핵심 예매 비즈니스 엣지 케이스 로직
실제 티켓팅 도메인의 로직 맹점을 방어(Safe)하는 비즈니스 코드입니다.
- **대기 인원 큐잉 및 랭킹 정렬 (Zset 활용)**: `server.py` **522 Line 부근** (`def ticket_enter_locked()`)
- **취소 시 대기실 튕김 방지 처리 (부분 무효화)**: `server.py` **639 Line 부근** (`def ticket_cancel_locked()`)
